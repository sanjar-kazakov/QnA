nav.navbar style="background-color: #e3f2fd;"
  .navbar-text.px-4
    - if user_signed_in?
      = link_to t('Sign out'), destroy_user_session_path, method: :delete, class: 'btn btn-outline-secondary btn-sm', data: { turbolinks: false }
